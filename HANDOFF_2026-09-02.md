# HANDOFF — chantier rockim_f2, état au 2026-09-02 ~10:00

**Pour reprendre dans une nouvelle session : lire ce fichier, puis `CHANTIER_f2.md` §7 et
`tunnel_schisto/SUITE_solutions.md`. Binaire courant : `rockim_f2j.exe` (suite fast 44/44).**

## 1. Ce qui tournait au moment du handoff

| quoi | état | où récupérer / relancer |
|---|---|---|
| Revue biblio « schistosité récente + FEMDEM » (workflow `wf_977defc2-9ae`, 13 agents) | **lancée 09:52, 0/13 rendus** — si la session est morte, les agents sont morts avec elle | script persistant : `~/.claude/projects/C--Users-fuzquianoalricabi-simulations/7e7c5111-23f3-4e3a-87dc-8684a34311a8/workflows/scripts/schistosite-femdem-recent-wf_977defc2-9ae.js` → relancer `Workflow({scriptPath: <ce fichier>})` (le `resumeFromRunId` ne vaut que dans la même session). Si elle a fini : résultat dans `.../tasks/wxn1eh7iy.output` (JSON, clé `doc`) ou `.../subagents/workflows/wf_977defc2-9ae/journal.jsonl` (ligne `label: liste`). À extraire vers `tunnel_schisto/BIBLIO_schistosite_femdem_recent.md` (voir le script d'extraction utilisé pour `REVUE_traversee_litage.md` dans l'historique : parser `journal.jsonl`, `type == result`). |
| Aucun run de production | machine libre | — |

## 2. Ce qui est fait et prouvé (tout opt-in, bit-identique clés absentes)

| capacité | clés | validation | doc |
|---|---|---|---|
| AbuAisha : `preBrokenJoints`, `microseismic`, `dampingViscous(±implicite)`, `hydroCavityClosure`, correctif crash `nVert_` | — | t1–t8, 28 fichiers bit-identiques, 44/44 | `DOCUMENTATION_rockim.md` §5.11 |
| Choc thermique : `thermal*`, Robin, `toolStart`, `dampingSwitchT` | — | conservation 1,4e-13, forme fermée 0,011 % | §5.12 |
| Plans discrets `weakPlanes` (+ continuité) | — | courbe en U de Jaeger, continuité | §5.13 |
| **3 briques de Lisjak** : `beddingDip`, `beddingEperp/NuPerp/Gperp` (TI), `beddingXxRatio` (loi γ, **min à γ=0** — coquille p. 102 de la thèse), `make_tunnel_bedded_mesh.py` v6 | — | forme fermée +0,00 %/+0,13 %, γ neutre bit-identique, continuité 1,005–1,018 | §5.14 |
| S8 `writeJointState` (contactState 0–5), S5 `weakPlaneFactor2/Frac2/Seed` | — | t17, t18, 44/44 | §5.15 |
| **Polydispersité des grains** (`rockim_f2l.exe`, 12:43) : `grainSizeSpread`, `phase.<nom>.grainSize` — Laguerre à aires prescrites (Newton amorti KMT 2019, Bourne 2020) | — | bit-identité deck GBM 8 fichiers ; 0,5 → 0,496, 0,8 → 0,802 réalisés, fractions conservées ; suite fast **44/44** (`suite_f2l.txt`) | §5.16, `calib_quick/fig_tess_polydisp.png` |

**État 16:00 — calibration triaxiale (lire `calib_quick/PLAN_calibration.md`).** Binaires : `rockim_f2m.exe` (clés de calibration, 44/44), `rockim_f2n.exe` (grainMeshRandom + disque Gmsh en brésilien, suite en cours). Enquête à 5 agents dans `calib_quick/enquete/` (A1 pétrographie 62/32/6 ρ 2640 ; A2 pratique FDEM-GBM ; A3 faits de code ; A4 méthode ; A5 critique adverse → corrections q = σ − σ₃, E physique). Outillage `calib_quick/calib/` : design, emulator (GP nugget), apso (Zhan 2009), objective (+BTS), extract (CI = 1ʳᵉ insertion, CD = 1ʳᵉ rupture, correction C0), make_decks (--bts), runner (1 × 14), joint_frames, plot_controls, **campaign.py** (init/collect/fit/optimize/enrich/report, testé 45/45 sur synthétique). Design h1 : `runs_h1/design_h1.csv` (50 pts, ft/c/φ/G_f/G_II·G_I). Contrôles rendus : bruit maillage négligeable ; vitesse ÷4 → pic −4 % ; base ×0,7 E 77,7 : pic 591 (−1 %) à 50, 380 (−6 %) à 20, 881 (+10 %) à 100 ; CI 0,64/0,54 ; **CD collé au pic partout** ; damping 0,3/0,1 → pic −4/−5 %, chute plus forte. En attente : pénalité d'insertion 20, intrinsèque, 40 × 80 mm, cas 3 GBM refait (`q3r`). **Rien de la calibration n'est lancé** (150 runs, une nuit, validation Fernando requise).

**Calibration rapide Red Bohus (après-midi, `calib_quick/README.md`) :** trois cas < 5 min à σ₃ = 50 (homogène 709 / Weibull 709 / GBM α=1 515 MPa vs 599 exp) sur maillage Delaunay algo 5 (le frontal algo 6 est **BANNI**, R6 = 0,34) ; balayage à G_f fixe = artefact, à ℓ_cz constant monotone (×0,5 → 547, ×0,35 → 496, ×0,25 → 439). Plan validé par Fernando : **trois calibrations** (homogène APSO/RF ; Weibull m × seeds ; GBM joints intra/inter-granulaires par phase + tailles de grain via la polydispersité), puis essais d'impact comparés. Choix ouverts : bulk élastique vs MC par phase (ajout solveur), cibles (fit 20/50, prédire 75/100), E ≈ 72 GPa pour 77 apparent (déformation plane).

Pièges documentés à ne pas repayer : `CHANTIER_f2.md` §7.1 (dt fixé par le plus petit triangle ET `pj = 4E/hmin` global en `mesh = file`), §7.2 (résultats du run 45°), §7.3 (solutions).

## 3. Résultat scientifique du seul run de production (`out_lisjak45_4h`, 4 h 11)

EDZ en losange : profondeur **6,0 m le long du litage / 3,4 m en travers (rapport 1,77 ; témoin isotrope 1,19)**. −31 % de joints rompus, EDZ p95 10,8 m (isotrope 20,3), 93 % de blocs mono-élément, convergence 3,4 m (effondrement). Rien ne traverse la frontière (plan délaminé). **Prédit** par He–Hutchinson avec Γ_i/Γ_b = 0,057 (seuil 1/4) ; rapports mesurés 0,10–0,30 ; S6 : ℓ_cz litage 179 mm < arête 200 mm (non résolue). Revue complète : `tunnel_schisto/REVUE_traversee_litage.md`. `edz_metrics.py` est **aveugle à 45°** → `tunnel_schisto/tools/edz_sectors.py`.

## 4. Plan des runs de validation (rien n'est lancé — validation Fernando avant chaque run)

Un job à la fois, 14 threads, depuis **son** terminal.

| étape | runs | heures | ce qu'on saura |
|---|---|---|---|
| **A** | `S4_ratios1`, `S4_isotrope_maillage_lite`, `S1_G030`, `S3_lambda130` — `powershell -ExecutionPolicy Bypass -File tunnel_schisto\run_solutions.ps1 S4_ratios1 S4_isotrope_maillage_lite S1_G030 S3_lambda130` | 17 | loi ou maillage ; rapport mesuré ; comparaison à Bure (Armand Table 2) |
| **B** | `tunnel_lisjak00_4h`, `tunnel_lisjak90_4h` — `run_lisjak_4h.ps1` (fait 45→0→90 ; 45 est déjà fait) | 8,5 | H1 : l'EDZ tourne-t-elle avec le pendage |
| **C** | AbuAisha `e4_kf_iso12`, `e4_kf_aniso`, `e11_bilan_iso12`, `e13_friction_iso12`, `e12_montney` (depuis la racine de `rockim/rockim_p1`, binaire `..\..\rockim_f2\rockim_f2j.exe`) | 8,5 | cœur du banc, seul point de terrain |
| **D** | `tunnel_lisjak45` (t = 0,35 / h = 0,12) | 17 | convergence en t, résout ℓ_cz |
| E | reste des `S*` (38 h), reste des `e*` (~25 h), **deck thermique de production à concevoir** (forage refroidi Robin → percussion `toolStart` ; cibles : Eα_TΔT/(1−ν), a/ξ, Bažant, Saksala 2020) | — | sensibilités |

Dépouillement : `tools/edz_sectors.py --dip 45`, `tools/joint_state_stats.py --dip 45` (exige `writeJointState`), `tools/tip_velocity.py`, p1 `block_sizes.py`, `crack_orientation.py` (depuis la racine de p1, chemin absolu).

## 5. Fichiers clés

`DOCUMENTATION_rockim.md` §5.11–5.15 · `CHANTIER_f2.md` §0–7.3 · `tunnel_schisto/{SUITE_solutions.md, REVUE_traversee_litage.md, BIBLIO_anisotropie_fdem.md, biblio/lisjak_thesis.pdf, tunnel_lisjak*_4h.cfg, S*.cfg, run_*.ps1, tools/}` · `meshes/tunnel_hs_bed{00,45,90}_t06.msh` (production), `tunnel_hs_bed45.msh` (fin) · `tests_f2/t1–t18` · `out_lisjak45_4h/` (résultats + `_planche_finale.png`).

## 5bis. Archivage fait le 2026-09-02 17:00 (demande Fernando)

- **Base `phd_geothermie`** : `FDEM/rockim_f2/` copié (sources, docs, decks, figures, enquête,
  outillage ; sans `out_*` ni sorties de test), `PHD.md` + `phd/FDEM.md` (§3quinquies) mis à
  jour ; commit **b1c23e2** poussé sur `origin/master` (438 fichiers). Hors git mais dans
  OneDrive : binaires `*.exe`, `tunnel_schisto/biblio/lisjak_thesis.pdf` (54 Mo), maillages
  `*.msh` (règle `.gitignore`).
- **GitHub rockim (Fuzquial/rockim)** : branche **`f2-2026-09-02`** créée depuis `origin/main`
  avec l'arbre f2 (commit 5076082, 360 fichiers), poussée ; `main` intact ; worktree local
  `FDEM/rockim/rockim_f2_wt`. La fusion dans `main` reste une décision de Fernando.

## 6. Règles en vigueur

Un job 14 cpus à la fois ; Fernando lance ; validation de la liste de clés + coût avant chaque run ; croissance par addition (rien retiré sauf faux ou mieux) ; bit-identité clés absentes prouvée à chaque greffe ; suite `fast` sur chaque binaire ; base `phd_geothermie` = archive (git à sa racine) — **rien n'a encore été copié vers la base ni commité** ; la promotion de `rockim_f2` en base attend la décision de Fernando.
