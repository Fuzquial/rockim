# HANDOFF — session du 2026-08-27 (verrou du noyau broye leve)

> **Pour une session Claude qui prend le relais** : lis ce fichier puis le §9
> de [BILAN_replique_solidity_2026-08-27.md](BILAN_replique_solidity_2026-08-27.md).
> Tout le detail y est ; ceci est la carte.

## Le resultat du jour, en 5 lignes

La plage d'adoucissement de mode II etait transcrite **3·G_II/c** (cohesion
seule, figee) alors que la forme publiee — these Guo 2014 p. 65 + eq.
2.24/2.30/2.33 (verifiees sur les pages), code Solidity Y3Dfd.c l. 1110-1126,
code Y ancetre, Y-Geo eq. 7.4 — divise par **fs = c + tanφ·|σn| a la pression
courante**. Sous l'insert (σn ~ GPa) l'ecart vaut ×54 : la rupture en
cisaillement des joints comprimes etait interdite → pas de noyau broye → pas
de fissures radiales (champ de Hertz elastique : σθθ comprime en surface) →
rebond 0,99. Ni le schema d'insertion (adaptatif ET intrinseque donnent 0
cisaillement avec la loi fausse), ni le maillage, ni la duree.

**Correctif** : cle `jointShearRange = coulomb` (opt-in, defaut `cohesion`
bit-identique octet a octet, 2D + 3D). Valide 3× le jour meme : indentation
2D St Anne (cisaillement du noyau ×6,2, champ lointain intact), banc
polyaxial de la these (c = 0,5 MPa : 0 rompu a 2,1× le seuil sans la cle,
11 893 rompus 100 % cis. avec), revue adverse independante (0 defaut).

## Etat des branches (rien d'ecrase, tout sauvegarde)

- **joint-handoff** (ce worktree, rockim_p4) : ea86c19 (etat du run du 26/08,
  sauvegarde = branche remote `backup/joint-handoff-pre-coulomb`) → d1c242b.
  Porte : la cle 2D+3D, `bench_polyaxial/`, `impact_imperial_coulomb.cfg`,
  l'addendum §9, `build_cl.cmd` (→ rockim_cl.exe).
- **claude/rockim-recent-mpka9c** (base main + 6 commits) : la cle cote base,
  le banc, la figure `bench_impact/figures/fig_loi_joint.pdf` (loi de joint
  adaptatif, 4 panneaux). **Merge dans main : a faire par Fernando.**
- **phd_geothermie**, meme nom de branche : entree datee dans phd/FDEM.md +
  ligne d'index PHD.md. Merge main : idem.

## En cours / a faire (ordre de priorite)

1. **LE run decisif** : `rockim_cl.exe bench_impact\configs\impact_imperial_coulomb.cfg out_imperial_coulomb`
   (nuit, ~10 h+). Verifier au lancement la banniere
   `[FDEM3D] jointShearRange = coulomb` (une cle mal orthographiee est
   IGNOREE en silence par le lecteur de config). Verdict A/B a t_sim ≈ 106 µs
   contre `out_imperial` (26/08). Predictions dans le bandeau du deck :
   bmode = 2 sous l'insert des t_sim ≈ 100-110 µs, frottement vers l'echelle
   32 J, restitution vers 0,72, noyau vers 195-200 µs, radiales 250-390 µs.
   Si cisaillement nul a 106 µs → le correctif est refute a historique egal.
2. **Depouillement** : `bench_impact/tools/imp_lib.py` — critere de rupture
   `tBreak >= 0` (JAMAIS `damage >= 0.999`, faux depuis jointFailRule =
   majority). Horloges : contact insert/roche vers t_sim ≈ 68-70 µs ; leur
   fig. 14 compte DEPUIS ce contact.
3. Integrer `bench_polyaxial/` au tier fast (critere de PASS dans son
   LISEZMOI) — c'est le repere compressed-shear-to-failure qui manquait.
4. Calibration eventuelle apres verdict : retouches fines de gfShearFactor
   (66,7 → 40-50) PAR-DESSUS la loi corrigee ; jamais de ÷33 uniforme
   (demonstration : run D, F-p plafonnee des 0,05 mm, champ lointain ×20).
5. Notes ouvertes : (a) un joint confine peut rompre A LA DECHARGE (plage
   suit la pression, smax irreversible) — physique a discuter au
   depouillement, pas un bug ; (b) `bulkDamage` defaut inerte sur calcaire
   (δ0 Kuru a recaler) ; (c) `law = mc` a cohesion macro eteint la
   fissuration (calibration ecole Ye requise).

## Regles a respecter (rappel des conventions du projet)

Capacites opt-in, defauts bit-identiques ; binaire DEDIE par lot, jamais
d'ecrasement d'un exe en cours de run ; UN gros run a la fois, lance par
Fernando dans SON terminal ; suite fast avant toute campagne ; figures PDF
vectoriel Computer Modern ; dater les bilans, ne jamais reecrire
l'historique ; ne rien supprimer sans accord.
