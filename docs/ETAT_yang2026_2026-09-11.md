# rockim face aux impacts d'Imperial (Solidity) — le point du 2026-09-11

**Question posée** : « à chaque fois on trouve une nouvelle erreur, une nouvelle chose à corriger,
et c'est infini. Qu'est-ce qui marche, qu'est-ce qui ne marche pas, qu'est-ce qui peut être
optimisé — et corrige tout une fois pour toutes. »

**Arbre** : `FDEM\rockim_g1`. **Binaire** : `rockim_g1y.exe` (sources modifiées le 11/09 au soir,
liste au §4). **Deck de référence** : `configs/yang2026_impact.cfg` (v2). **Banc court** :
`configs/yang2026_bench_s25.cfg`. Tout chiffre ci-dessous a été mesuré aujourd'hui sur cet arbre,
sauf mention « article » ou « bilan du <date> ».

---

## 1. Réponse en une page

**Ce qui marche.** Le noyau physique de Solidity est dans rockim, clé par clé, et chaque clé a été
vérifiée contre leur source publique ou leurs équations : joints cohésifs intrinsèques à six nœuds
(z-curve de Munjiza, branche élastique parabolique de Guo, plage de mode II divisée par
f_s(σ_n), mort du joint à la majorité des points), Mohr-Coulomb à coupure de traction (leur éq. 1),
DIF de compression et de traction aux formules exactes réévaluées à chaque pas sur le taux brut,
pulvérisation (leurs éq. 3-4, constantes de leur Table 1 en défaut), contact par potentiel de
Munjiza avec k_t/k_n = 2/7, frottement par matériau à la règle du minimum, effondrement de
portance des éléments endommagés (d_fact), naissance de contact à force continue, six corps
maillés (piston, bit, insert, circlip, plaque, roche) avec brasage insert/bit, jauge de
contrainte au milieu du bit, cinématique des corps, tri des fragments par anti-gravité (leur
§2.3 de 2025), bilan d'énergie à sept postes avec garde-fou d'arrêt. Le solveur refuse une clé
inconnue, une clé inerte, une combinaison sans effet. Le maillage de leur fig. 6 se génère en
une commande (120 185 tétraèdres contre leurs 230 788 — l'écart est le bit, qu'ils maillent à
3 mm et nous à ~6 mm : il est continu, l'onde y est 1D).

**Ce qui ne marchait pas — et pourquoi c'était « infini ».** Le deck `yang2026_impact.cfg` v1 du
11/09 matin avait été **réécrit de zéro** alors qu'un deck validé du 30/08 existait dans le même
arbre (`bench_impact/configs/impact_kuru9_intrinseque.cfg`) et avait déjà résolu la plupart de ses
défauts. La v1 portait **onze écarts** à la loi qu'elle prétendait reproduire à l'identique, dont
quatre auraient détruit le run avant la première fissure (§3) : un outil analytique fantôme, des
joints de roche (f_t = 11 MPa) dans l'acier et le carbure, un insert non brasé au bit, et un
contact nœud-face ~800 fois trop mou pour le choc piston/bit. Aucun n'était visible sur un run
de 2 µs — la roche n'est touchée qu'à 50 µs. **La boucle infinie n'est pas un défaut du code :
c'est l'absence d'un banc qui parcourt toute la chronologie en minutes avant de lancer 20 h.**
Le banc existe maintenant (§6).

**Ce qui peut être optimisé.** Le pas de temps (1,0 ns) est commandé par les **slivers** de la
roche (diamètre inscrit minimal 0,226 mm pour une médiane de 0,40 mm dans la zone fine) portant
des joints à pénalité 20 E/h ; la CFL seule autoriserait 4,7 ns. Trois leviers mesurés, cumulables
(§5) : optimisation du maillage (×1,5 attendu), `jointPenaltyFactor = 10` (×1,36, −4 % de module
apparent supplémentaire), `dtFactor = 0.2` (×1,33, la valeur des runs Kuru). Le coût par pas est
dominé par le contact général et par les joints, qui ne profitent ni l'un ni l'autre des 14 fils
(§5.2) : c'est le prochain chantier de code, pas de deck.

---

## 2. Comment le point a été fait

1. Lecture du vault (`CERVEAU` → `MOC-fdem-rockim` → trois notes ETUDES), du deck v1 et de son
   document `BANC_yang2026_impact.md`, de l'audit du 11/09 matin, des quatre articles (pages de
   méthode relues, pas seulement le résumé).
2. Relecture du **code** aux points que le deck sollicite : `computeStableDt`, `placeTool`,
   `setupBoundaries`, `assignJointProps`, `generalContact` / `potentialContact`, `integrate`,
   `jointForces`, lecture des clés et de leurs défauts.
3. **Mesures** (toutes sur `rockim_g1fix.exe` puis `rockim_g1y.exe`, 14 fils sauf mention) :
   journal complet du run court v1, profil par pas (`ROCKIM_PROF=1`), sept variantes de pas de
   temps, distribution des diamètres inscrits du maillage, banc court à l'échelle 2,5.
4. **Recherche de l'existant** : `bench_impact/configs/` contenait quatre decks Kuru du 30/08
   dont un intrinsèque, avec 9 des 11 corrections déjà écrites et commentées.

---

## 3. Les onze écarts de la v1, corrigés dans la v2

| # | écart (v1) | conséquence mesurée ou prévue | correction (v2) | déjà dans le deck Kuru du 30/08 ? |
|---|---|---|---|---|
| 1 | `toolShape` absent → sphère analytique 0,5 kg à 8 m/s créée en plus des six corps | +16 J au bilan, masse fantôme ; 1,9 ms avant de toucher | `toolShape = none` | oui |
| 2 | `phase.insert/bit/…` ne posaient que ρ, E, ν → **f_t = 10,98 MPa, c = 29,84 MPa hérités de la roche** dans l'acier et le carbure | le carbure se fissure sous le contact hertzien, le bit sous 178 MPa d'onde | `phase.steel/carbide.ft = cohesion = 1e12` **et** `groupContinuum` (§4) | oui (1e12) |
| 3 | insert et circlip **libres** (aucun `groupBond`) : « fragments : 6 » au journal | l'insert reste sur la roche au rebond, la vitesse de rebond du bit (critère 3) est celle du bit nu | `groupBond.bit.insert = joints`, `groupBond.bit.circlip = joints` | oui |
| 4 | contact général = pénalité nœud-face, k = 0,01 E h_min = 1,2·10⁶ N/m par nœud | choc piston/bit ~100 kN sur ~50 nœuds → pénétration 1,2 mm ≫ plafond 0,6 h_min = 0,12 mm : le piston traverse le bit | `contact = potential` (Munjiza, celui de Solidity), `potTangentFactor = 1.4286`, `gcBirth = penalty`, `gcActivation = adaptive` | oui |
| 5 | frottement 0,6 partout, 0,18 « résiduel » sur éléments pulvérisés | Table 1 : **0,18 est le coefficient de la roche**, 0,6 celui du carbure et de l'acier ; Solidity prend le min de la paire (Y3Did.c l. 1292) → tout contact avec la roche à 0,18 ; bilan du 03/09 : decks 3,3× trop hauts | `contactMu.rock = 0.18`, `.steel = .carbide = 0.6`, `contactDamageCoupling = solidity` | partiellement (documenté comme divergence) |
| 6 | `dampingLocal` = 0,05 (défaut percussion 3D), `jointXi` = 0,05 (défaut) | amortissement de Cundall sur l'onde du bit + amortisseur visqueux sur chaque joint : deux puits d'énergie absents de Solidity | `dampingLocal = 0`, `jointXi = 0` | dampingLocal oui, jointXi non |
| 7 | `jointShearRange` absent (défaut `cohesion`) | plage de mode II ×54 trop grande sous l'insert → pas de noyau broyé, rebond 0,99 (verdict du 27/08) | `jointShearRange = coulomb` | non (adaptatif) |
| 8 | `strainRateDIFArm = envelope` (gel à l'enveloppe) | Solidity réévalue le DIF à chaque pas sur le taux brut (Y3Dfd.c l. 1448-1456) ; le gel était « le choix le plus proche », pas le leur | `strainRateDIFArm = continuous`, `strainRateFilter = none` | non |
| 9 | `gauge.bit = 0.15 0.20` écrit dans le repère du fichier Gmsh | le solveur translate le maillage de +0,149 m : la fenêtre lisait les 26 mm du **bas** du bit (champ proche du contact), Yang mesure au **milieu** | `gauge.bit = 0.28 0.31` ; le journal imprime désormais l'étendue de chaque corps et avertit | oui |
| 10 | `crushCap` absent → plafond du déviateur à 8 c = 239 MPa (garde-fou maison) | plasticité parfaite sous l'insert où la pression dépasse le GPa ; absent de Solidity | `crushCap = 1e12` | oui (2e9) |
| 11 | jeux 0,2 mm piston/bit **et** insert/roche ; `gravity` absent ; pas de `budgetAbortPct` | 22 µs + 44 µs de vol pour rien (≈ 1,3 h) ; pesanteur absente (Yang l'applique) ; un run de 20 h qui crée de l'énergie finit « [OK] » | jeux 0,02 mm (`gap=`, `gapr=` du mailleur), `gravity = 9.81` + `energyBodyForces = on`, `budgetAbortPct = 2` | gravité et garde-fou oui |

Trois conventions de Solidity portées le 27/08 depuis leur source ont été ajoutées en plus :
`jointQuadrature = midedge`, `jointDeltaC = solidity`, `jointFailRule = majority`
(BILAN_replique_solidity du 27/08, six clés).

**Ce que le document `BANC_yang2026_impact.md` de la v1 disait de faux** : « l'algorithme de retrait
des fragments manque » — il existe (`fragBrushStart/V0/Accel/Beta`, `brushReport`) ; « retirer les
six lignes `phase.*.fraction` » — la clé est **obligatoire** (le deck serait refusé) ; « quatre
corrections en attente » — il y en avait onze.

---

## 4. Ce qui a été changé dans le code (opt-in, défaut bit-identique)

| fichier | changement | pourquoi |
|---|---|---|
| `Fdem3dSolver.hpp/.cpp` | **`groupContinuum.<corps> = true`** : les facettes intérieures d'un corps continu (et l'interface entre deux corps continus liés par `groupBond`) deviennent `Joint::perm` — jamais évaluées, jamais insérées, hors budget CFL ; la liaison de nœuds par groupes (celle de l'insertion adaptative) s'arme en schéma intrinsèque dès qu'il en existe | 39 803 facettes (17 %) sur 232 408 ne sont plus des ressorts ; l'acier et le carbure sont des continuums EF exacts ; la complaisance E/(1+1/pf) disparaît du train de frappe |
| `Fdem3dSolver.cpp` | avertissement **outil fantôme** : `groupVel` posé et `toolShape ≠ none` | écart n° 1 |
| `Material.hpp` | avertissement **résistances héritées** : `phase.<x>.E` posé, E > 1,5 E_global, ni `.ft` ni `.cohesion` posés | écart n° 2 |
| `Fdem3dSolver.cpp` | impression de la **translation du maillage et de l'étendue en z de chaque corps** ; avertissement si une jauge sort de la moitié centrale du corps | écart n° 9 |
| `Fdem3dSolver.cpp` | impression du **nœud qui commande le pas** (corps, phase, h inscrit, part élément / joints / contact outil) | §5.1 |
| `tools/make_impact_mesh.py` | arguments nommés `gap=`, `gapr=`, `opt=` (défauts inchangés) | écart n° 11, §5.1 |
| `tools/yang_report.py` | dépouillement des sept critères + santé du run depuis `history.csv` et le journal | §6 |
| `tools/keys_by_mode.json` | registre régénéré (préfixe `groupContinuum.`) | sans quoi la clé serait refusée |
| `DOCUMENTATION_rockim.md` | ligne `groupContinuum.<corps>` | |

Bit-identité : voir `results/bitid_g1y.json` (§7).

---

## 5. Optimisation : ce qui est mesuré, ce qui reste

### 5.1 Le pas de temps (deck v2, maillage s = 1, 120 185 tets)

| configuration | dt (ns) | gain | commentaire |
|---|---|---|---|
| v1 (joints partout, pf 20) | 0,942 | — | commandé par les joints du **carbure** (E 600 GPa, 0,7 mm) |
| v1, insert à E = 60 GPa (test) | 1,42 | ×1,51 | preuve que le carbure commandait |
| v1, pf = 5 | 1,75 | ×1,86 | |
| v1, `insertion = none` (CFL seule) | 3,28 → 4,67 sans terme outil | ×5 | le plafond |
| **v2 (acier et carbure continus)** | **1,001** | ×1,06 | commandé désormais par les **slivers de la roche** |
| v2, pf = 10 | 1,366 | ×1,36 | −4,3 % de module apparent supplémentaire |
| v2, `extraContacts = 0` | 1,033 | ×1,03 | le terme d'outil analytique (inexistant ici) pèse 3 % : négligé |

Diamètres inscrits (6V/A) du maillage s = 1 : roche min **0,226** mm, p1 0,401, p5 0,486, médiane
1,76 (zone fine ≈ 0,40) ; insert min 0,205, médiane 0,51 ; bit min 0,259, médiane 1,30. Le pas
des joints suit le **minimum** : un sliver sur cent mille commande le run. Levier :
`make_impact_mesh.py … opt=2` (deux passes Netgen + relocalisation), en cours de mesure (§7).

Yang annonce 2,5 ns pour 230 788 éléments. Avec maillage optimisé (×1,5 attendu) + pf 10 (×1,36) +
dtFactor 0,2 (×1,33), rockim serait à ~2,7 ns : leur cadence. Aucun des trois n'est gratuit :
le premier est neutre en physique, le deuxième change le module apparent, le troisième mange la
marge de stabilité (Xiang et al. 2009 avertissent sur les forces tangentielles). **Décision à
prendre par Fernando**, pas par défaut.

### 5.2 Le coût par pas (v1, 14 fils, 213 pas ; `ROCKIM_PROF=1`)

| poste | 14 fils | 7 fils | scaling |
|---|---|---|---|
| éléments | 11,3 ms | 20,9 ms | ×1,85 : parallèle |
| joints | 32,4 ms | 34,1 ms | **×1,05 : ne scale pas** |
| contact général | 50,3 ms | 52,1 ms | **×1,04 : ne scale pas** |
| outil | 0,6 ms | 1,0 ms | |
| **total** | **~95 ms** | ~108 ms | |

Les joints sont parallélisés par listes par fil (`fTL_`) mais la réduction et l'accès mémoire
dominent ; le contact est parallèle en détection et **série** en application. À 14 fils on paie
donc 14 cœurs pour 1,1× la vitesse d'un seul sur 85 % du temps. C'est le vrai chantier de coût,
et il est dans le code, pas dans le deck. Le contact de la v2 (potentiel + activation
adaptative) est mesuré au §7.

### 5.3 Ce qui n'est PAS à optimiser

- Le bit à ~6 mm au lieu de leurs 3 mm : continu, onde 1D, pulse de 520 mm — 6 mm suffisent.
- La plaque de charge et le circlip : 4 % des éléments, ne touchent rien en 800 µs (la plaque
  tombe de 3 nm sous g), gardés pour la fidélité au montage.

---

## 6. Le banc court : ce qu'il vérifie et pourquoi il manquait

`configs/yang2026_bench_s25.cfg` = le deck v2 avec le maillage s = 2,5 (10 563 tets, mêmes
six corps, mêmes jeux) : il parcourt piston → bit → insert → roche → rebond en minutes et
falsifie chaque mécanisme que la v1 ne pouvait pas voir à 2 µs :

| mécanisme | signature attendue | où la lire |
|---|---|---|
| choc piston/bit par potentiel | piston freiné de 9 à ~4 m/s, bit lancé, aucune traversée | `vz_piston`, `vz_bit` |
| onde dans le bit continu | σ_zz à la jauge ≈ 160 MPa (Yang) ; 1D : ρ c v/2 = 178 MPa | `szz_bit` |
| insert brasé | `vz_insert` = `vz_bit` au rebond | history |
| roche chargée dès ~50 µs | joints rompus > 0, pulvérisés > 0 | `nBroken`, `nPulv` |
| frottement 0,18 sur la roche | `eFric` < 0 croissant | history |
| rebond du bit | `vz_bit` change de signe, rapport rebond/indentation ~0,7 (Yang 9 m/s) | `vz_bit` |
| bilan d'énergie | résidu < 1 %, aucune création, pas d'arrêt `budgetAbortPct` | journal |
| acier/carbure intacts | 0 joint rompu hors roche (`joints intra/homo/hetero`) | journal |

Résultats : §7. Dépouillement : `python tools/yang_report.py out_yang_bench_s25 results/yang_bench_s25.log`.

---

## 7. Résultats des bancs du 11/09 (soir)

*(section remplie au fil des runs — voir aussi `results/`)*

---

## 8. Ce qui reste ouvert, honnêtement

1. **La raideur de pénalité n'est pas publiée** (vérifié sur les quatre PDF) ; 20 E/h est un
   choix, mesuré à −4,8 % de module apparent sur la roche (banc `configs_bench/pen_f*.cfg`).
2. **Leurs tétraèdres de volume sont des T10 à F-bar** (Xiang et al. 2009), rockim est en T4 ;
   leur élément joint est bien le même à six nœuds.
3. **La taxonomie des fissures** (médiane / latérale / radiale) n'existe pas dans
   `crater_metrics.py` : les critères 6-7 (radiale max, rayon de cratère) se lisent, la
   mécanique des latérales non.
4. **Le tri des fragments** existe mais prolonge le run de 0,5 ms (+62 %) : bloc commenté en fin
   de deck, à armer quand la masse de fragments est la métrique.
5. **`jointResidualMu = 0`** : Solidity ne porte aucun cisaillement sur un joint rompu
   (`dpefm = 0.0`, l. 1091). L'avertissement du solveur sur `jointShearUnload = origin` sans
   `jointFrictionScaled` ne s'applique pas quand le résiduel est posé (note du deck Kuru).
6. **Coût par pas** : joints et contact ne scalent pas avec les fils (§5.2).
7. **`bulkDamage = yang` n'est pas objectif au maillage** (bilan du 03/09) — c'est aussi vrai
   du modèle de Yang, calibré à 1 mm ; le deck s = 1 est à leur maille, le banc s = 2,5 n'est
   comparable qu'à lui-même.

---

## 9. Décisions pour Fernando (rien n'est lancé)

1. Lancer `rockim_g1y.exe configs/yang2026_impact.cfg out_yang2026` (coût au §7) ?
2. Prendre le maillage optimisé `impact_yang_s1_pose_opt2.msh` si le gain sur dt se confirme ?
3. `jointPenaltyFactor` 20 (choix v1/v2) ou 10 (Munjiza, dt ×1,36) ?
4. `dtFactor` 0,15 ou 0,2 (runs Kuru) ?
5. Durée : 8·10⁻⁴ s (sept critères) ou 2·10⁻⁴ s (onde, pic, début de fissuration) ?
6. Armer le tri des fragments (+0,5 ms) ?

---

## 10. Fichiers

| chemin | rôle |
|---|---|
| `configs/yang2026_impact.cfg` | deck v2, commenté ligne à ligne (v1 conservée dans git) |
| `configs/yang2026_bench_s25.cfg`, `_court.cfg` | banc court, 800 et 200 µs |
| `meshes/impact_yang_s1_pose.msh` | 120 185 tets, jeux 0,02 mm |
| `meshes/impact_yang_s1_pose_opt2.msh` | idem, deux passes d'optimisation |
| `meshes/impact_yang_s2.5_pose.msh` | banc, 10 563 tets |
| `tools/yang_report.py` | dépouillement |
| `results/yang_*.log`, `results/mesh_*.log`, `results/bitid_g1y.json` | mesures du jour |
| `docs/BANC_yang2026_impact.md` | document de la v1 (historique, non réécrit) |
